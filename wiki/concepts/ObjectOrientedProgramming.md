---
title: "Object-Oriented Programming"
type: concept
tags: [oop, design, python]
sources: [bubble-sort, trees, linked-list, oop-inheritance, oop-game, oop-flash-cards, oop-basics, abstraction-todo]
last_updated: 2026-04-09
---

# Object-Oriented Programming

A programming paradigm based on the concept of objects — data structures that contain both data (attributes) and code (methods). Core principles include encapsulation, inheritance, polymorphism, and abstraction.

## Appearances in Sources
- [[oop-basics]] — simplest example: `Song` class with `__init__` and instance method
- [[bubble-sort]] — `Line` and `LineList` classes encapsulate sort data and visualization behavior
- [[trees]] — `Node` and `BinaryTree` classes model tree structure and traversal
- [[linked-list]] — `LinkedList` and `Node` classes with full CRUD operations
- [[oop-inheritance]] — multi-level inheritance chain (Animal -> Lion -> Mufasa -> Simba)
- [[oop-game]] — Engine/Map/Scene architecture with inheritance and composition
- [[oop-flash-cards]] — drilling OOP syntax vocabulary (is-a, has-a, instance, parameter)
- [[abstraction-todo]] — architectural design using Document base class and separation of concerns

## Connections
- [[Inheritance]] — core OOP principle, heavily explored across sources
- [[BubbleSort]] — implemented using OOP classes
- [[BinaryTree]] — tree structure modeled with OOP
- [[LinkedList]] — data structure implemented with OOP
- [[Recursion]] — used within OOP methods for tree traversal and input validation
